#include "sphere.h"

Sphere::Sphere()
{
}

triangle_indice(int v1, int v2, int v3)
{

}

int
Sphere::AddVertex(Point p)
{
	double length = sqrt(p.x * p.x + p.y * p.y + p.z * p.z);
	geometry.push_back()
}
